# SSH credentials
connect-ssh.sh

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Build outputs
dist/
build/
*.o

# Dependencies
node_modules/

# Runtime data (archivos y reportes generados por el procesador en tiempo real).
# NUNCA commitear: pertenecen al usuario que corre el servicio y rompen el pull
# en el servidor por permisos de directorio.
services/api/data/
